output = fopen('fibOutput.csv', 'w') ;
fprintf('%s,', 'n', 'fib') ;
fprintf('%s\n', 'runtime') ;
for i = 1:35
    s = timeFib(i);
    m = {m; i, s.fib, s.runtime}
    dlmwrite('fibOutput.csv', m, '-append') ;
end

fprintf(output, '%s,', c{1,1:end-1}) ;
 fprintf(output, '%s\n', c{1,end}) ;
 fclose(output) ;
 dlmwrite('test.csv', c(2:end,:), '-append') ;